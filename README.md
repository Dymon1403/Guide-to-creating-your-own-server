# Правила создания автономного сервера


# 1. Введение
пр сегодня расскажу как сделать себе жизнь лучше просто используя старый вонючий комп, в жизни каждого наверно возникает такое ощущение что бля, как будто просто хочется отчасти стать автономным, буду стараться подробно все рассказать

# 2.Начала начал
перед установкой ОС и софта, важно все серьезно подготовтить,настоятельно рекомендую сделать:
  масс чистка комплектующих, будь комп или ноут, просто надо, сервер должен работать всегда, с новой термухой он просто прослужит дольше, так вот, все продуть и собрать, на проц залить новую термуху ЕСЛИ КОМП:
  ARCTIC MX-4 / MX-6 - абсолютный топ по соотношению цена/качество.MX-4 максимально легко наносится, MX-6 чуть гуще, но лучше справляется, если чип сильно греется. Не сохнет годами.
  Noctua NT-H1 / NT-H2 - надёжная классика. Отличная теплопроводность, долго живучая, легко намазывается.
  Thermal Grizzly Hydronaut - если хочется выжать максимум по градусам. (Важно: Kryonaut от этой же фирмы брать не стоит - она быстро деградирует при температурах выше 80°C, а Hydronaut держится отлично).
# notes
 Не бери дешевую поеботу "пасты-КПТ-8" или китайский шлак с вб/алика они высохнут через 2-3 месяца работы под постоянной нагрузкой, и придется снова всё разбирать.

для НОУТБУКА:
  для ноутбука выбор ТЕРМОПАСТЫ закрыт, ее тупо нельзя в нем использовать лол, потому что у мобильных процессоров и видеокарт нет теплораспределительной крышки (голый кремниевый кристалл), а прижим охлаждения слабее, так что надо юзать что-то круче, чето проктичее и созданное для ноутов спецтально - термопрокладки
  Honeywell PTM7950 - сука ну вот, самый самый мед для ноута, живет долго, наносится изи, фулл кайф, рекомендую

 ЕСЛИ НЕ ХОЧЕТСЯ ТЕРМОПРОКЛАДОК:
   Gelid GC-Extreme - термоПАСТА но супер супер вязкая, подходит для ноутов, и можно хоть на рынке у бабки найти

Отлично, с охладом закончили, дальше важный Note, если у вас ноут, если батарее плохо, не мучай себя и ноут, выкинь ее нахуй. если живая, то расскажу чуть позднее что будем с ней делать.

# 2.1 Настройка BIOS/UEFI:

Restore on AC Power Loss: Найди пункт Power Management -> AC Power Recovery (или аналогичный) и выставь Power On. Теперь, если дома моргнет свет, сервер автоматически включится сам при появлении питания. 
Отключение лишнего: В BIOS отключи встроенный звук (Audio), Bluetooth и неиспользуемые порты, если они не нужны - это сэкономит пару ватт и уменьшит нагрев, что для сервака гуд

# 2.2 WIFI Ethernet
 сука забудь нахуй навсегда про какие там сети, ток мужской кабель, wifi дает нестабильность + в linux мне потом спасибо скажешь, все автоматом подхватывается. Дальше, заходим в настройки роутера(192.168.0.1 or 192.168.0.1 standart) найди раздел DHCP Server -> Static Lease / Address Reservation и там дай stable IP твоему серверу, чтобы он всегда был доступен по одному и тому же адресу.

# 3. ОС и Базовая настройка системы
# 3.1 Какой дистрибутив брать?

Забудь про десктопные системы с графическим окружением (GNOME, KDE и прочие графические оболочки). Серверу не нужен монитор и лишний жор оперативы.
    Debian / Ubuntu Server - классика. Железобетонная стабильность, миллион статей в инете под любую проблему.
    Void Linux / Alpine - выбор для ценителей минимализма. Быстрый старт, минимальное потребление ОЗУ (Void без systemd на runit жрет буквально 50-70 МБ оперативы в простое).

# 3.2 Сетевое поведение крышки (если сервер на Ноутбуке)

Чтобы ноут не уходил в сон при закрытии крышки, нужно жестко прописать поведение обработчика событий:

    В systemd (Debian/Ubuntu):
    Редактируем /etc/systemd/logind.conf:

    HandleLidSwitch=ignore
    HandleLidSwitchExternalPower=ignore
    
    И применяем: sudo systemctl restart systemd-logind

    В runit / elogind (Void Linux):
    Редактируем /etc/elogind/logind.conf и выставляем те же параметры HandleLidSwitch=ignore.

  # 3.3 Настройка батареи (ноутбук)
  Если батарея живая разумным вариантом будет поставить на ней отграничения по зарядке, поставь максимум гдето 60-80%, оптимальный вариант для лития. Акум умрет не так быстро от постоянной зарядки
  есть много способов ограничения зарядки литийинонного аккумулятора, например TLP:
  
  *P.S. в гайде буду говорить сугубо про systemd дистры, например выше перечисленные Ubuntu Server или Debian*

итак, ставим TLP:
``` bash
  sudo apt update && sudo apt install tlp

```
заходим в конфиг:
```bash
sudo vim /etc/tlp.conf

```
здесь нам надо раскомпилить 2 строчки:
```bash
START_CHARGE_THRESH_BAT0=60
STOP_CHARGE_THRESH_BAT0=70

```
*START_CHARGE_THRESH_BAT0 - Заряд ПРИ котором начинается зарядка акума, ну и второй параметр STOP_CHARGE_THRESH_BAT0= - соответственно за ограничение зарядки ноута.*
# Применение настроек батареи
```bash
sudo tlp start

```
проверка, подхватилось ли все?
``` bash
sudo tlp-stat -b

```
В выводе в секции Charge Thresholds ты должен увидеть свои значения 60 и 70.  

# 3.3.1 Что делать если ноут не поддерживает пороги аккума? 

Итак при запуске sudo tlp-stat -b ты видишь надпись:
Notice: battery charge thresholds not supported for this model. значит твой ноут просто не умеет пользоваться контроллером питания(или его просто тупо нету)

Тут есть 2 варианта:
1. Аппаратный Способ, просто вытащи батарею
2. Программный Способ, тк акуммулятор не умеет контролирвать верхний порог питания, то мы просто будем контролировать нижний порог, чтобы сервер не рубился в ноль когда акум сядет в ноль:
  Когда отключают свет, ноут переходит на батарею. Если свет не дали за 5–10 минут, сервер должен самостоятельно и корректно завершить работу (shutdown), сохраняя все базы данных и конфиги.
# Как это настроить(скрипт-автомат):
1. Создаем скрипт контроля питания
```bash
sudo nvim /usr/local/bin/power-monitor.sh

```
2. Код
```bash
#!/bin/bash

# ПРОВЕРЬ СУКА СВОЙ АКУМ, КАК ОН НАЗЫВАЕТСЯ
# На разных системах может быть AC, ADP1 или ACAD. Проверить можно через: ls /sys/class/power_supply/
AC_STATUS=$(cat /sys/class/power_supply/AC/online 2>/dev/null || cat /sys/class/power_supply/ADP1/online)

# Если AC_STATUS = 0, значит внешнее питание пропало
if [ "$AC_STATUS" -eq 0 ]; then
    # Ждем 3600 секунд
    sleep 3600

    STILL_OFF=$(cat /sys/class/power_supply/AC/online 2>/dev/null || cat /sys/class/power_supply/ADP1/online)

    if [ "$STILL_OFF" -eq 0 ]; then
        # Свет так и не дали - ну все пизда
        /sbin/shutdown -h now 
    fi
fi
```
3. Делаем скрипт исполняемым:
```bash
sudo chmod +x /usr/local/bin/power-monitor.sh

```
4. Закидываем в планировщик (cron), чтобы выполнялся раз в минуту:
```bash
sudo crontab -e

```
окей, с аккумулятором и первоначальной настройкой системы закончили, наша система готова к бою.

# 4 Безопасный SSH-доступ, ключи Ed25519 и изоляция root-пользователя.
1. Генерация криптографических ключей Ed25519. Забудь про логины и стандартные пароли — вход должен быть строго по SSH-ключам. Алгоритм Ed25519 — современный стандарт: он быстрее, безопаснее и компактнее старого RSA
2. На своем основном ПК/ноутбуке (с которого будешь подключаться к серверу) открываешь терминал и генерируешь пару ключей:
```bash
# (Утилита спросит, куда сохранить ключ — жми Enter по умолчанию. Также можно задать кодовую фразу passphrase для дополнительной защиты).
ssh-keygen -t ed25519 -C "main-laptop"

```
3. Копируешь публичный ключ на сервер:
```bash
# (Вместо user — имя твоего пользователя на сервере, вместо 192.168.1.100 — статический IP сервера который ты указывать ранее(пункт 2.2)).
ssh-copy-id -i ~/.ssh/id_ed25519.pub user@192.168.1.100

```
4. Проверяешь чтобы вход был без пароля:
```bash
ssh user@192.168.1.100

```

Шаг 2: Настройка демона SSH (sshd_config)

Теперь, когда вход по ключу настроен, полностью закрываем забор для входа по паролям и логина от имени root:
1. Открываешь конфиг-файл SSH НА СЕРВЕРЕ:
```bash
sudo nvim /etc/ssh/sshd_config

```
2. Находишь (или добавляешь в конец файла) и жестко прописываешь следующие директивы:
```bash
# Запрещаем вход от имени root напрямую
PermitRootLogin no

# Отключаем аутентификацию по обычным паролям (только ключи!)
PasswordAuthentication no
PubkeyAuthentication yes

# Запрещаем пустые пароли
PermitEmptyPasswords no

# Отключаем устаревшие методы аутентификации
KbdInteractiveAuthentication no

# Смена дефолтного порта (по желанию, защищает от тупого скана ботов)
Port 2222
```
3. Сохраняешь файл и проверяешь синтаксис конфига на ошибки перед перезапуском, чтобы не заблокировать сам себя:
```bash
sudo sshd -t

```
Если вывод пустой, то все гуд

4. Перезапускаешь службу SSH:
```bash
sudo systemctl restart ssh

```
5. ВАЖНО: Не закрывая текущую сессию терминала, открой новое окно на рабочем ПК и проверь подключение с учетом нового порта:
```bash
ssh -p 2222 user@192.168.1.100
```
Шаг 3: Автоматическая блокировка сканеров (Fail2ban)(необязательный)
Если планируешь пробрасывать SSH наружу (или просто для защиты в локалке), поставь утилиту fail2ban. Она банит IP-адреса, с которых идут подозрительные или ошибочные попытки авторизации.
# Install
```bash
sudo apt install Fail2ban

```
создавай локал конфиг /etc/fail2ban/jail.local и прописывай там:
```bash
[sshd]
enabled = true
port = 2222
filter = sshd
logpath = /var/log/auth.log
maxretry = 3
bantime = 1d

```
терь на твоем порту после 3 неудачных попыток ввода IP улетит в бан на сутки

